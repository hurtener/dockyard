# markdownlint-cli2 configuration for Dockyard docs.
default: true

# Line length: prose in the RFC and briefs wraps generously; don't fight it.
MD013: false

# Allow inline HTML (tables, details, etc. in design docs).
MD033: false

# First line need not be a top-level heading (briefs carry front-matter-style metadata).
MD041: false

# Permit duplicate headings under different parents (per-phase detail blocks).
MD024:
  siblings_only: true

# Table pipe-spacing/alignment style: cosmetic only. MD060 is new in
# markdownlint v0.40.0 and post-dates the RFC, README, and vendored-spec tables;
# do not fight it.
MD060: false

# Bare URLs are normal in research-brief Sources lists and vendored-spec READMEs.
MD034: false

# Hard tabs: Go code samples in fenced blocks legitimately use tabs — only flag
# tabs in prose, never in code.
MD010:
  code_blocks: false
