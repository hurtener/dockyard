// Package authz provides the transport-independent authorization foundation for
// Dockyard protected resources. HTTP integration supplies a bearer token to a
// Validator and places the resulting Principal in the request context.
package authz
